********************
id coordinate source
********************



For a given set of coordinates, the n closest points from the four quadrants,
plus more close points if n > 4. The input set of coordinates is either the
coordinates found within the bounding box, or n nearest neighbors if no
coordinates exist within the box.