models:
  AssetInput:
    constructors:
      - name: minimal
        components: [uuid, id]
        superArgs: [uuid]
      - name: full
        components: [uuid, id, operator, operationTime]
        superArgs: [uuid]
